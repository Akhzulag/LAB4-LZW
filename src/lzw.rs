use std::fs::File;
use std::io::Result;

pub fn decode(file_read: &str, file_write: &str) -> Result<()> {
    Ok(())
}

pub fn encode(file_read: &str, file_write: &str) -> Result<()> {
    Ok(())
}

fn main() {
}
